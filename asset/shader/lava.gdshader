shader_type canvas_item;


uniform sampler2D pattern : filter_nearest, repeat_enable;


void fragment() {
	//vec4 color = texture(pattern, SCREEN_UV);
	//if (COLOR.a != 0.0 && color.a != 0.0) {
		//COLOR = color;
	//}
}